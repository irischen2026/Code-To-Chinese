# Changelog
All notable changes to the WutZit project will be documented in this file.

---

## [1.0.0] - 2026-06-16

### 🚀 核心特性与交互封装

#### 1. 双模态解释引擎 (Dual-Mode Reasoning)
- 实现了“一般模式”与“专家模式”的一键 Toggle 切换。
- **一般模式**：采用大白话解释，剔除复杂的学术定义，严禁指出代码 Bug。
- **专家模式**：自动启用深度技术审计，强制遵循 `【逻辑分析】+【已知隐患】+【改进方案】` 的三段式输出格式。
- **懒加载计算原则**：禁止预加载运算，仅在触发模式切换事件时拉取当前模式的数据，并在切换至专家模式时显示显眼的运算深度增加加载动画。

#### 2. 轻量级意图分类门禁 (Semantic Gatekeeper)
- 增加了前置“意图过滤器”：在执行正式解码调用前，通过极速 Prompt 分类器检测文本。
- 只有属于编程、软件工程或技术开发的内容才被允许放行；非技术文本将被门禁安全婉拒并提示：`此处内容非技术逻辑，WutZit 拒接受理`。

#### 3. Token 实时审计栏 (Token Realtime Audit Bar)
- 对接了各大模型厂商的 API Token 使用量统计接口（Gemini 的 `totalTokenCount`，DeepSeek 与 OpenAI 的 `total_tokens`）。
- 在气泡框右下方增加系统元数据呈现栏，以极小字号（10px）浅灰色等宽字体显示本次交互的实际消耗，并在重新计算或追问时自动刷新。

#### 4. 极致视觉减法 UI
- 移除了聊天气泡的多余边框、背景阴影和指向小箭头，实现头像与文本的“无缝融合”。
- 规范化左右边距为 12px，使其在视觉上呈现印刷精美的极客卡片风格。
- **零符号污染**：大模型输出中剔除星号（**）和反引号（`）等 Markdown 标记。通过中括号 `[ 重点词 ]` 在前端智能渲染为精致的灰色胶囊高亮代码块。

#### 5. 工程化与安全性加固
- 支持 Tauri 本地加密存储（`Store`）与 Vite 环境变量本地双轨加载机制（支持 `.env`）。
- 增加了启动加载时 settings 存储读取的 2秒超时保护 race 机制，防止因为权限锁死导致界面卡在空白加载态。
- 引入全局 JS 错误捕获动态横幅，实时捕获未处理的 React 奔溃并提供友好提示。
- 整理并输出了标准的 `.env.example` 占位模版和防泄漏 `.gitignore`。
